# 腾讯云部署指南

## 项目3D功能说明
本项目已集成完整的3D模型展示功能，基于以下技术：
- **Three.js** - 3D图形渲染引擎
- **React Three Fiber** - React集成
- **React Three Drei** - 3D组件库

## 部署前准备

### 1. 构建生产版本
```bash
npm run build
```
✅ 已完成 - 生成了 `build` 文件夹

## 腾讯云部署步骤

### 方案A：COS静态网站托管（推荐）

#### 步骤1：创建COS存储桶
1. 登录[腾讯云控制台](https://console.cloud.tencent.com/)
2. 进入对象存储COS服务
3. 创建存储桶：
   - 名称：`plc-education-web`（自定义）
   - 地域：选择离目标用户最近的地域
   - 访问权限：**公有读私有写**

#### 步骤2：配置静态网站
1. 进入存储桶配置
2. 找到"静态网站"设置
3. 开启静态网站功能：
   - 索引文档：`index.html`
   - 错误文档：`index.html`（React单页应用需要）
   - 重定向规则（可选）

#### 步骤3：上传文件
1. 使用COS控制台或COSBrowser工具
2. 上传整个 `build` 文件夹内的所有内容
3. 确保文件权限为"公有读"

#### 步骤4：访问网站
- 访问地址格式：`https://[bucket-name].cos.[region].myqcloud.com`
- 例如：`https://plc-education-web.cos.ap-guangzhou.myqcloud.com`

### 方案B：云服务器部署（更灵活）

#### 使用Nginx部署
```bash
# 1. 安装Nginx
sudo apt update && sudo apt install nginx

# 2. 复制build文件到服务器
scp -r build/* root@your-server-ip:/var/www/plc-education

# 3. 配置Nginx
sudo nano /etc/nginx/sites-available/plc-education
```

Nginx配置文件：
```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /var/www/plc-education;
    index index.html;

    # React Router支持
    location / {
        try_files $uri /index.html;
    }

    # 3D模型文件缓存
    location ~* \.(obj|mtl|gltf|glb|fbx)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 7d;
        add_header Cache-Control "public";
    }
}
```

#### 启用网站
```bash
sudo ln -s /etc/nginx/sites-available/plc-education /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

## CDN加速配置（可选但推荐）

### 为什么需要CDN？
- 加速3D模型加载
- 提升全球访问速度
- 减少服务器负载

### 配置步骤
1. 在腾讯云CDN控制台添加域名
2. 源站类型选择COS或自有源
3. 配置缓存规则：
   - HTML文件：不缓存
   - JS/CSS：7天
   - 3D模型文件：30天
   - 图片/视频：30天

## 自定义域名配置（可选）

1. 购买域名（腾讯云域名注册）
2. 完成ICP备案（中国大陆必需）
3. 在COS或CDN中配置自定义域名
4. 添加DNS解析记录

## 部署后验证

### 检查清单
- [ ] 首页正常加载
- [ ] 3D模型正常显示和交互
- [ ] 路由切换正常
- [ ] 视频播放正常
- [ ] 响应式布局正常

### 3D功能测试
1. 访问包含3D模型的页面
2. 检查模型加载速度
3. 测试鼠标交互（旋转、缩放）
4. 验证光照和材质效果

## 性能优化建议

### 3D模型优化
- 使用压缩的模型格式（GLTF/GLB）
- 实现模型懒加载
- 使用LOD（细节层次）技术

### 网络优化
- 启用Gzip压缩
- 使用CDN加速
- 实现资源预加载

## 常见问题

### Q: 3D模型加载失败？
A: 检查：
- 模型文件路径是否正确
- CORS配置是否允许跨域
- 文件权限是否为公有读

### Q: 页面刷新404？
A: 确保静态网站配置中错误文档设置为 `index.html`

### Q: 访问速度慢？
A: 建议配置CDN加速，特别是3D模型文件

## 费用估算

### COS存储
- 存储费用：约 0.12元/GB/月
- 流量费用：约 0.5元/GB
- 请求费用：约 0.01元/万次

### CDN（如使用）
- 流量费用：约 0.21元/GB（国内）

## 联系支持
如遇到问题，可通过以下方式获取帮助：
- 腾讯云工单系统
- 腾讯云社区论坛
- 官方文档中心